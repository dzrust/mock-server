{
  "name": "mock-server-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t dzrust/mock-server-server:$npm_package_version .",
    "docker:start": "docker run -d -p 9001:8080 dzrust/mock-server-server:$npm_package_version",
    "docker:deploy": "docker push dzrust/mock-server-server:$npm_package_version"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.21.2"
  }
}
